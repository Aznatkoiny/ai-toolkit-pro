# Expedition #1 (qlib) — draft eval-task specs for suite v2

Input to chunk 2 (plugin v2 + suite v2 re-arm). 10 specs.

## T25 — alpha-forecasting-routing: cross-sectional ranking ask must route INTO the quant-finance domain (not book-default LSTM, not OUT-OF-SCOPE), sub-route to spatial/cross-sectional models, and disclose the scope-boundary conflict (evals/tasks/T25)

**Instruction sketch:** /dl-model-advisor:advise We follow ~500 liquid large-caps with daily OHLCV and fundamentals. Each month we want to rank all 500 by expected relative return, hold the top 50, and judge success by a backtest net of transaction costs. What should we build? End state: recommendation as a model plan in MODEL_PLAN.md (plan only — no starter code). Do not access the network. Stop after at most 25 turns. [No mention of citation/status/evidence line formats — natural problem only.]

**Graders:** workspace file_exists MODEL_PLAN.md; file_contains '^Status: IN-SCOPE'; file_contains 'Cited rule:.*\[domains/quant-finance/alpha-forecasting-routing\]'; file_contains '^Evidence: unverified'; file_contains '(?i)(cross-?section|spatial|graph|GNN|attention).{0,120}(rank|relative)' (spatial sub-route for relative ranking, per the concept's task-geometry table); file_contains '(?i)(cost-aware|transaction cost)'; file_contains scope-notes disclosure '(?i)scope-boundary' (draft conflicts with stable foundations/scope-boundary — stable wins, disclosed); state_check 'test ! -f starter_model.py && echo CLEAN' expect CLEAN; transcript max_turns 25. trials 3, metric pass^k, timeout 600.

**Red-safety:** Three failure layers. Bare Claude: gives plausible quant advice but never emits the plan contract — no '^Status:' line, no 'Cited rule: … [domains/quant-finance/alpha-forecasting-routing]' bracket ID (the ID exists only in this catalog), no '^Evidence: unverified' tier line. v1 plugin without the catalog: quant-finance is explicitly OUT-OF-SCOPE in SKILL.md's scope gate, failing the IN-SCOPE grader. Plugin+catalog minus this concept: lint_outputs.py blocks citations of nonexistent concepts, so the citation grader can only pass once this draft lands.

## T26 — alpha-forecasting-routing: order-execution ask must sub-route by paradigm to RL (not supervised forecasting), name nested multi-granularity optimization, and disclose the RL scope-boundary conflict (evals/tasks/T26)

**Instruction sketch:** /dl-model-advisor:advise Our daily stock-selection signals are decent; losses come from execution. We need to decide how to slice parent orders into child orders across the trading day — size, price, and timing — to minimize cost, and eventually optimize the daily strategy and the intraday executor together. What kind of model is this and what should we build? End state: plan in MODEL_PLAN.md, plan only. No network. <=25 turns.

**Graders:** file_contains '(?i)reinforcement' (paradigm sub-route: execution is RL, not supervised forecasting); file_contains '(?i)nested' (Nested Decision Execution framing for daily strategy + intraday executor); file_contains 'Cited rule:.*\[domains/quant-finance/alpha-forecasting-routing\]'; file_contains '^Evidence: unverified'; file_contains '(?i)scope-boundary' in Scope notes (stable lists RL as uncovered; stable wins, disclosed); state_check 'test ! -f starter_model.py && echo CLEAN'; optional rubric judge, one dimension: 'plan frames execution as a sequential decision problem rather than return forecasting, with the draft tier stated' Y/N/Unknown. trials 3, pass^k.

**Red-safety:** Bare Claude may well say 'use RL' (common knowledge), so content regexes alone are not red-safe — the anchors are the catalog markers: bracket concept ID, '^Evidence: unverified', and the Scope-notes disclosure that stable foundations/scope-boundary lists RL as uncovered (the 'stable wins, disclosed' protocol is a house behavior bare Claude cannot know). v1 plugin refuses RL outright with the OUT-OF-SCOPE status line, failing the citation and paradigm graders.

## T27 — leakage-and-evaluation: shuffled k-fold on a large financial panel plus latest-restatement fundamentals must be replaced by chronological/walk-forward splits, PIT discipline, IC-family signal metrics, and multi-seed reporting (evals/tasks/T27)

**Instruction sketch:** Fixtures: eval_setup.py using sklearn KFold(n_splits=5, shuffle=True) over a ~750k-row daily stock panel (12 years, CSI300-like), joining quarterly fundamentals on their latest restated values; DATA.md describing the panel. Instruction: /dl-model-advisor:advise Here is how we evaluate our return model (eval_setup.py, DATA.md). The dataset is large (~750k rows) so we went with 5-fold CV. Before we trust the numbers, review our evaluation design and give us the protocol we should use. End state: evaluation-focused model plan in MODEL_PLAN.md, plan only. No network. <=25 turns.

**Graders:** file_contains '^Protocol:.*(?i)(chronolog|walk-?forward)'; state_check negative "! grep -qiE '^Protocol:.*k-fold' MODEL_PLAN.md && echo CLEAN" expect CLEAN (the size-table's k-fold/holdout row is ruled out for financial panels); file_contains '(?i)(point-?in-?time|PIT|as-?of)' (restatement leakage); file_contains '(?i)(information coefficient|rank ?ic)'; file_contains '(?i)seed' (multi-seed reporting); file_contains 'Cited rule:.*\[domains/quant-finance/leakage-and-evaluation\]'; file_contains '^Evidence: unverified'; file_contains 'evaluation-protocol-by-size' (relation to the stable foundation stated as extension, not override). trials 3, pass^k.

**Red-safety:** Chronological splits are guessable by bare Claude, but the graders require the full catalog bundle: canonical '^Protocol:' plan line, bracket concept ID, '^Evidence: unverified', the named relation to foundations/evaluation-protocol-by-size, plus PIT+IC+multi-seed together. v1 plugin's stable size rule prescribes 'simple holdout > ~10,000 samples' and carries no PIT/IC/seed clauses and no quant concept to cite, so plugin-without-this-concept fails too; bare Claude emits none of the labeled lines.

## T28 — leakage-and-evaluation: implausibly high single-seed IC 0.15 must be calibrated against the sourced ~0.07/0.04 noise floor and trigger a leakage/PIT/multi-seed audit before capital allocation (evals/tasks/T28)

**Instruction sketch:** Fixture: RESULTS.md — new factor model, IC 0.15 on a CSI300-like universe 2019–2024, single run/seed, fundamentals joined as-latest, no transaction costs. Instruction: /dl-model-advisor:advise Our new model reports IC 0.15 (see RESULTS.md) and leadership wants to allocate next month. Sanity-check whether these results are believable and what we must verify first; write your review to EVAL_REVIEW.md. No network. <=25 turns.

**Graders:** file_contains EVAL_REVIEW.md '0\.07' and '0\.04' (noise-floor calibration scoped to the sourced 2023 CSI300/CSI500 results); file_contains '(?i)(leak|look-?ahead|point-?in-?time)'; file_contains '(?i)seed'; file_contains '(?i)(transaction|cost)'; file_contains 'Cited rule:.*\[domains/quant-finance/leakage-and-evaluation\]'; file_contains '^Evidence: unverified'; rubric judge, one dimension: 'review concludes IC 0.15 is implausibly high relative to the sourced noise floor and requires a leakage audit BEFORE allocation' Y/N/Unknown. trials 3, pass^k.

**Red-safety:** The ≈0.0725/0.0438 best-IC noise-floor anchors are catalog-sourced (AlphaGen 2023 tables, carried only by this concept); bare Claude can say '0.15 seems high' but cannot produce the scoped figures, the bracket concept ID, or the Evidence tier line. v1 plugin has no quantitative-finance evaluation content at all (scope-gated out), so it fails the citation and calibration graders.

## T29 — qlib-model-selection: engineered tabular factors must surface the GBDT-baseline draft AND the conflicting stable Dense-stack foundation with both trust tiers stated (dual-surfacing, 'stable wins, disclosed') (evals/tasks/T29)

**Instruction sketch:** /dl-model-advisor:advise We compute 158 hand-engineered factors per stock per day (tabular; some NaNs) to rank a cross-section of ~800 stocks by next-month return; ~2M rows total. My instinct is to reach for a deep MLP first. What should we train first? End state: plan in MODEL_PLAN.md, plan only — no starter code. No network. <=25 turns.

**Graders:** file_contains '(?i)(LightGBM|XGBoost|CatBoost|GBDT|gradient.?boost|boosted.?tree)'; file_contains '\[domains/quant-finance/qlib-model-selection\]'; file_contains '\[foundations/(dense-tabular|modality-architecture-map)\]' (the stable side of the recorded conflict must also be cited); file_contains '(?i)human-reviewed' AND file_contains '(?i)unverified' (both tiers surfaced on Evidence/Scope lines — no self-promotion of the draft); file_contains '(?i)(nan|missing)' (GBDT tolerates NaN, MLP breaks — preprocessing-per-family clause); state_check 'test ! -f starter_model.py && echo CLEAN'; rubric judge (one dimension): 'plan presents the tree-vs-dense conflict per stable-wins-disclosed without stating draft benchmark numbers as settled fact' Y/N/Unknown. trials 3, pass^k.

**Red-safety:** Bare Claude would likely also say 'start with LightGBM', so content alone is not red-safe. The red anchors are the catalog contract: bracket concept IDs on BOTH sides of the recorded conflict, dual Evidence tier lines (book-canon human-reviewed vs draft unverified), and the Scope-notes disclosure behavior — none of which exist outside plugin+catalog. v1 plugin routes vector/tabular to a Dense stack with no GBDT mention and no concept IDs, failing the GBDT and citation graders.

## T30 — qlib-model-selection: cross-version leaderboard comparison must be rejected (v0.8.0 backtest break), with 20-seed mean±std reporting and the under-tuning caveat applied (evals/tasks/T30)

**Instruction sketch:** Fixture: BENCHMARK_NOTES.md — team compared their single-seed LSTM backtest (qlib 0.9.x) to LightGBM numbers copied from a 2021 blog post's qlib 0.7 leaderboard and concluded 'our LSTM beats LightGBM'. Instruction: /dl-model-advisor:advise We think our LSTM is ahead of the published qlib leaderboard (BENCHMARK_NOTES.md). Review whether this comparison holds and tell us how we should report results going forward; write the review to BENCH_REVIEW.md. No network. <=25 turns.

**Graders:** file_contains BENCH_REVIEW.md '0\.8\.0' (backtests not comparable across the v0.8.0 change); file_contains '(?i)20\s+(random\s+)?seeds|mean.{0,6}(±|and).{0,6}std' (house benchmark protocol); file_contains '(?i)(not comparable|incomparab)'; file_contains '(?i)(limited resources|under-?tun)' (leaderboard-is-not-a-definitive-ranking caveat); file_contains 'Cited rule:.*\[domains/quant-finance/qlib-model-selection\]'; file_contains '^Evidence: unverified'. trials 3, pass^k.

**Red-safety:** The v0.8.0 backtest-break fact, the 20-seed mean±std protocol, and the maintainers' under-tuning caveat are qlib-benchmarks specifics carried only by this concept. Bare Claude produces a generic 'apples-to-oranges' warning that fails the '0\.8\.0' and 20-seed regexes and emits no Cited rule/Evidence lines; v1 plugin has no qlib content and would scope-gate or book-route the ask.

## T31 — qlib-qrun-workflow: reproducibility ask must yield the declarative qrun YAML pattern with the canonical reference frictions, not another one-off script (evals/tasks/T31)

**Instruction sketch:** /dl-model-advisor:advise Our team keeps writing one-off pandas scripts to test China A-share alpha models and results never reproduce. Set us up with a reproducible experiment definition for a LightGBM model on daily A-share data with a realistic cost-aware backtest — something a teammate can rerun with one command. End state: the experiment config file plus a short plan note in MODEL_PLAN.md. No network. <=25 turns.

**Graders:** state_check 'ls *.yaml | head -1' non-empty, plus 'python3 -c "import yaml,glob; yaml.safe_load(open(sorted(glob.glob(chr(42)+chr(46)+chr(121)+chr(97)+chr(109)+chr(108)))[0]))" && echo PARSES' expect PARSES (sketch — config must be valid YAML); file_contains config 'TopkDropoutStrategy', 'topk:\s*50', 'n_drop:\s*5', 'limit_threshold:\s*0\.095', 'open_cost:\s*0\.0005', 'close_cost:\s*0\.0015', 'min_cost:\s*5' (canonical reference frictions); file_contains MODEL_PLAN.md 'qrun', 'Cited rule:.*\[domains/quant-finance/qlib-qrun-workflow\]', '^Evidence: unverified', '(?i)yahoo' (bundled-data caveat: methodology comparisons, not production signals). trials 3, pass^k.

**Red-safety:** Bare Claude might recall fragments of qlib's example config from pretraining, so red-safety does not rest on the YAML alone: the graders jointly require the full canonical friction set (0.095/0.0005/0.0015/min_cost 5 + topk 50/n_drop 5), the MODEL_PLAN.md contract, the bracket concept ID, the Evidence tier line, and the Yahoo-data caveat — a conjunction bare Claude does not hit, with network denied. v1 plugin would emit Keras starter code (its Step 4 forbids non-keras stacks), never a qrun YAML plan.

## T32 — tsfm-financial-forecasting: commercial desk shortlisting Moirai must hit the licensing gate (CC-BY-NC research-only), be redirected to Apache-2.0 Chronos-2, and get the out-of-domain validation gate (chronological split + naive baseline) (evals/tasks/T32)

**Instruction sketch:** /dl-model-advisor:advise We are a prop trading desk (commercial use). For next-week daily close forecasts on ~40 tickers we shortlisted Salesforce Moirai 2.0 off a blog post recommending zero-shot time-series foundation models. Validate that choice or tell us what to use instead, including how we would prove it works before risking money. End state: plan in MODEL_PLAN.md, plan only. No network. <=25 turns.

**Graders:** file_contains '(?i)(cc-?by-?nc|non-?commercial|research (purposes )?only)' (Moirai blocked for commercial deployment); file_contains '(?i)chronos-?2' AND '(?i)apache' (default pick with license); file_contains '(?i)(out-of-domain|no financial (market )?data.{0,60}pretrain|pretrain.{0,60}no financial)'; file_contains '^Common-sense baseline:.*(?i)(last|naive|persistence)' AND '(?i)chronolog' (validation gate via the stable recurrent-forecasting foundation); file_contains 'Cited rule:.*\[modern/tsfm-financial-forecasting\]'; file_contains '^Evidence: unverified'; rubric judge (one dimension): 'plan rejects Moirai for this commercial use on license grounds and requires local validation before deployment' Y/N/Unknown. trials 3, pass^k.

**Red-safety:** The 2026-08 snapshot facts (Moirai CC-BY-NC-4.0 research-only across both generations, Chronos-2 as Apache-2.0 download leader, no financial data in the described pretraining mixes) sit at or beyond bare Claude's reliable knowledge with network denied — and even a lucky guess fails the canonical markers: bracket concept ID, '^Evidence: unverified', and the labeled baseline/protocol lines. v1 plugin knows no TSFMs and would prescribe a book-canon LSTM, failing every TSFM grader.

## T33 — tsfm-financial-forecasting: Granite TTM r2 on daily bars must be flagged (r2 is minutely–hourly; daily needs r2.1; external per-channel scaling mandatory) with finance-native Kronos surfaced incl. its 512-context truncation caveat (evals/tasks/T33)

**Instruction sketch:** /dl-model-advisor:advise We started integrating ibm-granite/granite-timeseries-ttm-r2 for daily-bar equity forecasts, feeding raw unscaled OHLCV. A teammate says there is a finance-specific foundation model we should evaluate instead. Is anything wrong with our setup, and what should we use? End state: plan in MODEL_PLAN.md, plan only. No network. <=25 turns.

**Graders:** file_contains '(?i)r2\.1' (daily/weekly support added only in r2.1); file_contains '(?i)(minutely|hourly)' (r2 resolution envelope); file_contains '(?i)(standard.?scal|z-?score|per-?channel)' (mandatory external scaling — raw OHLCV violates the card); file_contains '(?i)kronos' AND '512' (finance-native alternative with max_context truncation caveat); file_contains 'Cited rule:.*\[modern/tsfm-financial-forecasting\]'; file_contains '^Evidence: unverified'. trials 3, pass^k.

**Red-safety:** r2-vs-r2.1 resolution support, the per-channel external-scaling requirement, and Kronos's 512-token silent-truncation limit are model-card minutiae from 2025–2026 HF snapshots that bare Claude does not reliably hold (network denied blocks lookup). Even partial recall fails the conjunction plus the plugin-canonical bracket-ID citation and Evidence tier line; v1 plugin has zero foundation-model content.

## T34 — llm-automated-factor-research: a factor-automation plan built on single-alpha-IC mining, correlation-based dedup, and pasting raw prices to the LLM must be rewritten — marginal-contribution objective, correlation≠redundancy, schema-only LLM exposure, maturity hedges (evals/tasks/T34)

**Instruction sketch:** Fixture: FACTOR_PLAN.md — team's intended pipeline: (1) prompt an LLM with three years of raw daily price rows plus the train/test split dates so it 'understands the data'; (2) mine factors one at a time, keeping any factor whose own IC clears a bar; (3) drop any new factor correlated >0.9 with an existing one as redundant. Instruction: /dl-model-advisor:advise Review our plan for LLM-assisted alpha factor research (FACTOR_PLAN.md) before we build it, and rewrite the approach wherever it is wrong. End state: revised recommendation in MODEL_PLAN.md. No network. <=25 turns.

**Graders:** file_contains '(?i)(marginal contribution|contribution to the (combined |)pool|pool.{0,50}performance)' (reward pool-marginal contribution, not single-alpha IC); file_contains '(?i)schema.?(level|only)' (LLM never sees raw market data or split boundaries); file_contains '(?i)correlat' AND '(?i)redundan' (high mutual IC does not imply redundancy); file_contains '(?i)(self-?reported|independent replication|early stage|not settled)' (maturity hedge — draft numbers are single-team results); file_contains 'Cited rule:.*\[modern/llm-automated-factor-research\]'; file_contains '^Evidence: unverified'; rubric judge (one dimension): 'plan explicitly forbids exposing raw market data or temporal split dates to the LLM and replaces the per-factor IC objective with a pool-marginal objective' Y/N/Unknown. trials 3, pass^k.

**Red-safety:** The pool-marginal-contribution objective (AlphaGen), the mutual-IC-0.9746 correlation≠redundancy finding, and the schema-only leakage protocol (RD-Agent(Q)) are niche 2023–2025 paper results bundled only in this concept. Bare Claude's generic review ('watch out for leakage and overfitting') misses the specific pattern regexes and produces no bracket concept ID or Evidence tier line; v1 plugin would scope-gate LLM-agent workflows out entirely as post-2021.

