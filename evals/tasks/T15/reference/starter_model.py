"""Starter: small-data image classifier (feature extraction, Keras 3).

Generated by dl-model-advisor from MODEL_PLAN.md — adapt paths and image
size to your dataset before training. Loss per plan:
sparse_categorical_crossentropy.
"""
import keras
from keras import layers

NUM_CLASSES = 6
IMG_SIZE = (180, 180)

augmentation = keras.Sequential([
    layers.RandomFlip("horizontal"),
    layers.RandomRotation(0.1),
    layers.RandomZoom(0.2),
])

base = keras.applications.Xception(
    weights="imagenet", include_top=False, input_shape=IMG_SIZE + (3,))
base.trainable = False

inputs = keras.Input(shape=IMG_SIZE + (3,))
x = augmentation(inputs)
x = keras.applications.xception.preprocess_input(x)
x = base(x, training=False)
x = layers.GlobalAveragePooling2D()(x)
x = layers.Dropout(0.25)(x)
outputs = layers.Dense(NUM_CLASSES, activation="softmax")(x)
model = keras.Model(inputs, outputs)

model.compile(optimizer="rmsprop",
              loss="sparse_categorical_crossentropy",
              metrics=["accuracy"])

if __name__ == "__main__":
    model.summary()
