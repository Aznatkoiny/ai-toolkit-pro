I can't give you a book-grounded plan for this one, and it's worth being precise about why.

What you're asking for is parameter-efficient fine-tuning — LoRA adapters on a pretrained Llama-3-8B — so the model answers in your company's voice. My knowledge base is Deep Learning with Python, 2nd Edition, published in 2021. That book predates Llama, LoRA, and instruction tuning entirely: it never discusses adapting a pretrained billion-parameter language model, parameter-efficient methods, or preference/instruction datasets. There is no chapter I can honestly cite for this.

The closest material the book does contain is chapter 11's Transformer encoder/decoder built from scratch and chapter 12's character-level text generation with temperature sampling. Those are useful background for understanding attention and autoregressive decoding, but they train small models from random initialization on a single corpus — a different activity from adapting Llama-3-8B with low-rank adapters. Following the ch11-12 recipes would give you a small from-scratch language model, not a fine-tuned Llama, so I won't dress them up as an answer to your request, and I have not generated a starter training script.

I wrote MODEL_PLAN.md recording this: the status line marks the request OUT-OF-SCOPE for the book, the framing section captures your actual goal (LoRA on 50,000 support transcripts), and the adjacent-material section lists the ch11-12 content with an explicit warning that it is background reading only. For the real implementation, look at current PEFT/LoRA documentation and recent parameter-efficient fine-tuning literature rather than anything I can cite from 2021.
